NAMESPACE: "dfroehli-opendj-{{STAGE}}"
DNS_BASENAME: "{{STAGE}}.opendj.io"
GIT_REF: "master"

# KAFKA STUFF:
kafkaOperatorIsAvailable: true
KAFKA_TOPIC_CMD:  oc rsh backend-eventstore-zookeeper-0 /opt/kafka/bin/kafka-topics.sh --zookeeper localhost:21810 
KAFKA_NUM_BROKERS: 1
KAFKA_NUM_ZOOKEEPERS: 1
