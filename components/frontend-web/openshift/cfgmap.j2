apiVersion: v1
kind: ConfigMap
metadata:
    name: frontend-web-config-map
    namespace: "{{NAMESPACE}}"
data:
    config.json: |-
        {
        "enableDebug": true,
        "curatorPassword": "test",
        "playlistMaxSize": 50,
        "websocketHost": "http://{{DNS_BASENAME}}/" ,
        "websocketPath": "/api/service-web/socket.io",
        "SPOTIFY_PROVIDER_API": "http://{{DNS_BASENAME}}/api/provider-spotify/v1" ,
        "PLAYLIST_PROVIDER_API": "http://{{DNS_BASENAME}}/api/service-playlist/v1" 
        }
