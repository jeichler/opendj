apiVersion: v1
kind: ConfigMap
metadata:
    name: frontend-web-config-map
    namespace: "{{ENV.COMMON.NAMESPACE|mandatory}}"
data:
    config.json: |-
        {
        "enableDebug": true,
        "curatorPassword": "test",
        "playlistMaxSize": 50,
        "websocketHost": "http://{{ENV.COMMON.DNS_BASENAME|mandatory}}/" ,
        "websocketPath": "/api/service-web/socket.io",
        "SPOTIFY_PROVIDER_API": "http://{{ENV.COMMON.DNS_BASENAME|mandatory}}/api/provider-spotify/v1" ,
        "PLAYLIST_PROVIDER_API": "http://{{ENV.COMMON.DNS_BASENAME|mandatory}}/api/service-playlist/v1" 
        }
